model {
  extend execution {
    funnel = component {
      title "TES Engine"
      description "TES compliant job engine"
      technology "e.g. Funnel, TESK"
      icon /logos/tes.png

      -> protected_data 'Queries protected data'
      -> tre.s3 'Stores job result'
    }
  }
}

views {
  view execution of execution {
    title 'TRE / Execution Service'
    include *
  }
}
